from setuptools import setup, find_packages

setup(
    name="dummy-package",
    version="0.1.0",
    description="A dummy package to satisfy LangGraph Cloud build requirements",
    packages=find_packages(),
    install_requires=[],
)
